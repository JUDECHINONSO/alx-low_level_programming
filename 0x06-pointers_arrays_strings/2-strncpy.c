#include "main.h"
#include <stdio.h>

/**
 * _strncpy -> copies a string
 * @dest: param1
 * @src: param2
 * @n: param3
 * Return: string
 */
char *_strncpy(char *dest, char *src, int n);
{

	strncpy(dest, srn, n);
	return (dest);
}
